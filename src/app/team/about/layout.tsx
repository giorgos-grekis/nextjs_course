import React, { type PropsWithChildren } from "react";

const TeamAboutLayout = ({ children }: PropsWithChildren) => {
  return (
    <>
      <h1>About Page Layout 2</h1>
      {children}
    </>
  );
};

export default TeamAboutLayout;
