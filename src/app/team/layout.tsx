import React, { PropsWithChildren } from 'react'

const TeamLayout = ({ children }: PropsWithChildren) => {
  return (
    <>
    <h1>About Page Layout</h1>
    {children}
  </>
  )
}

export default TeamLayout